# Server Configuration
PORT=3000
NODE_ENV=development

# Image Processing Configuration
# Default Euclidean distance threshold for color matching (0 = exact match)
# Higher values allow more color variation (recommended: 0-50)
TOLERANCE=0

# Gemini API Configuration
# Get your API key from: https://aistudio.google.com/apikey
GOOGLE_GEMINI_API_KEY=your_api_key_here
GEMINI_MODEL=gemini-3-pro-preview
GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview
GEMINI_API_TIMEOUT=600000

# Replicate API Configuration
# Get your API token from: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=your_replicate_api_token_here

# Supabase Configuration
# For local development: Run `supabase start` to get local credentials
# For production: Get from https://supabase.com/dashboard/project/[project-ref]/settings/api

# Project URL
# Local: http://127.0.0.1:54321
# Production: https://[project-ref].supabase.co
SUPABASE_URL=http://127.0.0.1:54321

# Anon/Public key (safe for client-side, respects RLS)
# Format: JWT token starting with eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Service role key (server-side only, bypasses RLS)
# ⚠️  KEEP SECRET - Never expose to client-side code or commit to git
# Format: JWT token starting with eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_KEY=your_supabase_service_key_here
